# Phising-URL-detection
A machine learning-based phishing URL detection system that classifies malicious and legitimate URLs using a Random Forest model trained on URL lexical and structural features, achieving 90.24% accuracy.
